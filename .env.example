# Copy to .env and fill in real values (also set these in Vercel for production).
# MongoDB connection string for the existing ~4,000-game database.
MONGODB_URI=

# IGDB (Twitch) credentials for fallback search + enrichment.
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
